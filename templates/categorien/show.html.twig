{% extends 'base.html.twig' %}

{% block title %}Categorien{% endblock %}

{% block body %}
    <h1>Categorien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorien.id }}</td>
            </tr>
            <tr>
                <th>Gerecht</th>
                <td>{{ categorien.gerecht }}</td>
            </tr>
            <tr>
                <th>Dranken</th>
                <td>{{ categorien.dranken }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('Categorien_index') }}">back to list</a>

    <a href="{{ path('Categorien_edit', {'id': categorien.id}) }}">edit</a>

    {{ include('Categorien/_delete_form.html.twig') }}
{% endblock %}
